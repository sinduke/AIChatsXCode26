# macOS Finder 元数据。
.DS_Store

# Swift Package Manager 与常规构建产物。
.build/
build/
DerivedData/

# xcode-build-server 为本机绝对路径生成的配置，不提交到仓库。
buildServer.json

# SweetPad 本机状态与缓存。
.sweetpad/

# Xcode 的个人界面和断点状态；共享工程设置仍保留在版本控制中。
xcuserdata/
*.xcuserstate
